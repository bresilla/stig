# /tmp/test_tparam.hpp

## Classes

### `Container&lt;T, Allocator&gt;`

```cpp
template<typename T, typename Allocator>
class Container {
};
```

A container class

**Template Parameters:**
- `T` (typename): The element type stored in the container
- `Allocator` (typename): The memory allocator to use (default: std::allocator<T>)

---

