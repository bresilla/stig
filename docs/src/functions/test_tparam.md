# /tmp/test_tparam.hpp

## Functions

### `distance&lt;T&gt;`

```cpp
template<typename T>
T distance(Point<T> a, Point<T> b);
```

Computes the distance between two points

**Parameters:**
- `a` (Point&lt;T&gt;): First point
- `b` (Point&lt;T&gt;): Second point

**Returns:** (T) The distance

**Template Parameters:**
- `T` (typename): The scalar type

---

